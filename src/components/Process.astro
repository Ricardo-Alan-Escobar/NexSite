---
const steps = [
  { number: "01", title: "Descubrimiento", description: "Desarrollamos aplicaciones web dinámicas y responsivas con tecnología de vanguardia para ofrecer soluciones efectivas y atractivas." },
  { number: "02", title: "Planificación", description: "Nuestro equipo crea una hoja de ruta detallada con cronogramas, hitos y asignación de recursos." },
  { number: "03", title: "Desarrollo", description: "Utilizando metodologías ágiles, construimos tu solución con actualizaciones periódicas y ciclos de retroalimentación." },
  { number: "04", title: "Entrega y soporte", description: "Garantizamos un despliegue exitoso con soporte continuo y optimización constante." },
];
---

<style>
  .step-card {
    opacity: 0;
    transform: translateY(50px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .step-card.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .step-card:hover {
    transform: scale(1.05);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
  }
</style>

<section id="Enfoque" class="w-full py-12 md:py-12 lg:py-12 bg-blue-50">
  <div class="container mx-auto px-4 md:px-6">
    <div class="text-center mb-12">
      <div class="inline-block rounded-lg bg-blue-100 px-3 py-1 text-sm text-blue-600 mb-4 border-blue-600">
        Nuestro enfoque
      </div>
      <h2 class="text-3xl font-bold tracking-tighter sm:text-5xl">
        ¿Cómo <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-700 to-blue-400">Trabajamos?</span>
      </h2>
      <p class="mx-auto max-w-[700px] text-gray-500 md:text-xl mt-4">
        Nuestro proceso de desarrollo comprobado garantiza transparencia, calidad y entrega oportuna.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {steps.map((step, index) => (
        <div class="relative p-6 overflow-hidden border border-blue-200 rounded-lg step-card" data-index={index}>
          <div class="absolute -right-4 -top-4 text-8xl font-bold text-blue-100">{step.number}</div>
          <div class="relative z-10">
            <h3 class="text-xl font-bold mb-2">{step.title}</h3>
            <p class="text-gray-500">{step.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const cards = document.querySelectorAll(".step-card");

    const revealOnScroll = () => {
      const triggerBottom = window.innerHeight * 0.85;
      cards.forEach((card, index) => {
        const cardTop = card.getBoundingClientRect().top;
        if (cardTop < triggerBottom) {
          card.classList.add("visible");
        }
      });
    };

    window.addEventListener("scroll", revealOnScroll);
    revealOnScroll(); // Ejecutar en carga inicial
  });
</script>
